# Copy to .env.local for local development (never commit secrets).

# Required for live AI companion replies (see src/app/api/companion/route.ts).
OPENAI_API_KEY=

# Optional: OpenAI-compatible API base URL.
# OPENAI_BASE_URL=
